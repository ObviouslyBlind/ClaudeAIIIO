# Example binaries stripped

Vendor graphics skills shipped HDR/EXR/PNG/JPG/GLB example assets (~52 MB).
Those files were deleted after install so this repo stays small.

Kept: `SKILL.md`, `references/`, example **source** (`.js` / `.ts` / `.md`).

Need a missing texture for an example? Fetch it from the upstream GitHub repo
named in `SOURCES.md`. Do not scaffold a new Vite game to hold it.
