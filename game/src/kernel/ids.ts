/** Stable ids for the shard kernel. PAPER. Not live accounts. */

export const KERNEL_VERSION = "K.1";
export const VISITOR_ID = "visitor";
export const NPC_ID = "npc";
/** Soft cap for the player table. Not 500 live sockets. */
export const PLAYER_CAP = 500;

export type PlayerId = string;
export type PlotId = string;
