/**
 * Tap an NPC stall to buy 1 good at lastPrice (same /api/buy as the market).
 * Lead stub — builder fills. Do not add a wallet.
 */
export function createStalls(_opts) {
  return {
    handleRay() {
      return false;
    },
    tick() {},
  };
}
