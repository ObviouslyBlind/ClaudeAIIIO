# Gauntlet status

Loop is **autonomous**. Critic = pixels only. Tests = lead/builder.
Critics must open a **new query** (`/?g=…`). Do not reuse a tab already on `/`.

## Held (pixel-ratified)

- Inland spawn + black tarmac + PAPER HUD
- Traffic, taxi overlay, ferry ticket, RMB orbit
- **Develop catalogue:** picker lists House / Shop / House with shop / Farm / Warehouse / Factory with PAPER costs (`/?g=cat18`). Unaffordable rows grey out. Cancel returns to the harbour.

## Next piece

**Interiors.** Lease a cheap lot, Develop → House, tap the parcel to place it, then **Enter** (or tap the building). PASS if a PAPER downstairs room appears (harbour hidden, not deleted). **Exit** returns to that plot.

Do not rewrite `traffic.js`, island centres, or PLAN.md.
